name: Build application and create GitHub release
on:
  push:
    tags: [ 'v*.*.*' ]
  workflow_dispatch:
  
jobs:
  build:
    uses: ./.github/workflows/build-msix-sideload.yml
    secrets: inherit

  release:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
         
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          
      - name: List downloaded files
        run: ls -R artifacts

      - name: Zip x86 package
        run: |
          zip -r artifacts/internals-viewer-msix-x86.zip artifacts/msix-package-x86/

      - name: Zip x64 package
        run: |
          zip -r artifacts/internals-viewer-msix-x64.zip artifacts/msix-package-x64/
          
      - name: Create release
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          gh release create "${GITHUB_REF_NAME}" artifacts/internals-viewer-*.zip --generate-notes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
