<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="InternalsViewer.UI.App.vNext.Controls.Connections.ServerConnectionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:winUi="using:CommunityToolkit.WinUI"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d">
    <Control.Resources>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
    </Control.Resources>
    <StackPanel Spacing="6" Background="Transparent" >
        <controls:SettingsCard Description="Name or network address of the SQL Server instance"
                               Header="Instance Name"
                               HeaderIcon="{winUi:FontIcon Glyph=e770}"
                               CornerRadius="4,4,0,0">
            <TextBox Text="{x:Bind ViewModel.InstanceName, Mode=TwoWay}" MinWidth="200" />
        </controls:SettingsCard>

        <controls:SettingsCard Header="Authentication"
                               Description="Authentication method"
                               Margin="0,-8,0,0"
                               CornerRadius="0">
            <ComboBox x:Name="AuthenticationProtocolsComboBox"
                      ItemsSource="{x:Bind ViewModel.AuthenticationTypes, Mode=OneWay}"
                      SelectedValuePath="Key"
                      DisplayMemberPath="Value"
                      SelectedValue="{x:Bind ViewModel.AuthenticationType, Mode=TwoWay}"
                      MinWidth="200" />
        </controls:SettingsCard>

        <controls:SettingsCard x:Name="UserIdSettingsCard"
                       Header="User Id"
                       Margin="0,-8,0,0"
                       CornerRadius="0"
                       IsEnabled="{x:Bind ViewModel.IsUserIdEnabled, Mode=OneWay}">

            <TextBox Text="{x:Bind ViewModel.UserId, Mode=TwoWay}"
                 MinWidth="200" />
        </controls:SettingsCard>

        <controls:SettingsCard x:Name="PasswordSettingsCard"
                       Header="Password"
                       Margin="0,-8,0,0"
                       CornerRadius="0"
                       IsEnabled="{x:Bind ViewModel.IsPasswordEnabled, Mode=OneWay}"
                     >

            <PasswordBox Password="{x:Bind ViewModel.Password, Mode=TwoWay}"
                         MinWidth="200" />
        </controls:SettingsCard>

        <controls:SettingsCard Description="Name of the database" 
                               Header="Database" 
                               Margin="0,-8,0,0"
                               CornerRadius="0">
            <StackPanel>
                <ComboBox DropDownOpened="DatabaseComboBox_DropDownOpened"
                          ItemsSource="{x:Bind ViewModel.Databases, Mode=OneWay}"
                          Text="{x:Bind ViewModel.Database, Mode=TwoWay}"
                          IsEditable="True"
                          MinWidth="200" />
            </StackPanel>
        </controls:SettingsCard>

        <controls:SettingsCard Header="Connect to a server instance"
                               Margin="0,-8,0,0"
                               CornerRadius="0,0,4,4">
            <Button Content="Connect"
                    Name="ConnectButton"
                    MinWidth="200"
                   
                    IsEnabled="{x:Bind ViewModel.HasErrors, Mode=OneWay,Converter={StaticResource BoolNegationConverter}}"
                    Click="ConnectButton_Click"
                Style="{StaticResource AccentButtonStyle}">
         
            </Button>
        </controls:SettingsCard>
    </StackPanel>
</UserControl>
