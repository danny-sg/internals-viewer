<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="InternalsViewer.UI.App.Controls.Plan.PlanNodePropertiesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:models="using:InternalsViewer.UI.App.Models"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:predicates="using:InternalsViewer.UI.App.Controls.Predicates"
    xmlns:selectors="using:InternalsViewer.UI.App.Helpers.Selectors"
    xmlns:converters="using:InternalsViewer.UI.App.Helpers.Converters"
    xmlns:plan="using:InternalsViewer.UI.App.Helpers.Converters.Plan"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TreeViewItem">
            <Setter Property="Padding" Value="8,4,0,4" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Background" Value="{ThemeResource LayerOnMicaBaseAltFillColorDefault}" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid x:Name="ContentPresenterGrid"
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}"
                              Margin="{ThemeResource TreeViewItemPresenterMargin}"
                              Padding="{ThemeResource TreeViewItemPresenterPadding}"
                              CornerRadius="{TemplateBinding CornerRadius}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundPointerOver}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundPointerOver}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundPointerOver}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushPointerOver}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Grid x:Name="MultiSelectGrid"
                                  Margin="{ThemeResource TreeViewItemMultiSelectSelectedItemBorderMargin}"
                                  Padding="0"
                                  BorderBrush="Transparent"
                                  BorderThickness="{ThemeResource TreeViewItemBorderThemeThickness}"
                                  CornerRadius="{ThemeResource ControlCornerRadius}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <Grid x:Name="ExpandCollapseChevron" Grid.Column="0" Padding="0" MinWidth="20" Opacity="{TemplateBinding GlyphOpacity}" Background="Transparent">
                                    <TextBlock x:Name="CollapsedGlyph" Foreground="{TemplateBinding GlyphBrush}" Width="Auto" Height="12" Padding="8,0,0,0" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TreeViewItemTemplateSettings.CollapsedGlyphVisibility}" FontSize="{TemplateBinding GlyphSize}" Text="{TemplateBinding CollapsedGlyph}" FontFamily="{StaticResource SymbolThemeFontFamily}" VerticalAlignment="Center" AutomationProperties.AccessibilityView="Raw" IsTextScaleFactorEnabled="False" IsHitTestVisible="False" />
                                    <TextBlock x:Name="ExpandedGlyph" Foreground="{TemplateBinding GlyphBrush}" Width="Auto" Height="12" Padding="8,0,0,0" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TreeViewItemTemplateSettings.ExpandedGlyphVisibility}" FontSize="{TemplateBinding GlyphSize}" Text="{TemplateBinding ExpandedGlyph}" FontFamily="{StaticResource SymbolThemeFontFamily}" VerticalAlignment="Center" AutomationProperties.AccessibilityView="Raw" IsTextScaleFactorEnabled="False" IsHitTestVisible="False" />
                                </Grid>
                                <ContentPresenter x:Name="ContentPresenter" Grid.Column="1" ContentTransitions="{TemplateBinding ContentTransitions}" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" MinHeight="20" Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <converters:BoolToFontFamilyConverter x:Key="BoolToFontFamilyConverter" />
        <plan:PlanPropertyBrushConverter x:Key="PlanPropertyBrushConverter" />
        <converters:DepthToIndentConverter x:Key="DepthToIndentConverter" />

        <DataTemplate x:Key="PropertyItemTemplate" x:DataType="muxc:TreeViewNode">
            <controls:DataRow HorizontalAlignment="Stretch">
                <TextBlock Text="{x:Bind ((models:PlanNodeProperty)Content).Name}"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           FontFamily="{x:Bind ((models:PlanNodeProperty)Content).IsNameMonospace, Converter={StaticResource BoolToFontFamilyConverter}}"
                           Margin="{x:Bind ((models:PlanNodeProperty)Content).Depth, Converter={StaticResource DepthToIndentConverter}}"
                           VerticalAlignment="Center"
                           TextTrimming="WordEllipsis" />
                <TextBlock Text="{x:Bind ((models:PlanNodeProperty)Content).Value}"
                           FontSize="12"
                           FontFamily="{x:Bind ((models:PlanNodeProperty)Content).IsValueMonospace, Converter={StaticResource BoolToFontFamilyConverter}}"
                           Foreground="{x:Bind ((models:PlanNodeProperty)Content), Converter={StaticResource PlanPropertyBrushConverter}}"
                           VerticalAlignment="Center"
                           ToolTipService.ToolTip="{x:Bind ((models:PlanNodeProperty)Content).Tooltip}"
                           TextWrapping="Wrap" />
            </controls:DataRow>
        </DataTemplate>

        <DataTemplate x:Key="PredicateItemTemplate" x:DataType="muxc:TreeViewNode">
            <controls:DataRow HorizontalAlignment="Stretch">
                <TextBlock Text="{x:Bind ((models:PlanNodeProperty)Content).Name}"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Margin="{x:Bind ((models:PlanNodeProperty)Content).Depth, Converter={StaticResource DepthToIndentConverter}, ConverterParameter=4}"
                           VerticalAlignment="Center"
                           TextTrimming="WordEllipsis" />
                <predicates:PredicateTextBox Text="{x:Bind ((models:PlanNodeProperty)Content).Predicate}"
                                             TextPadding="0"
                                             HasBackground="False"
                                             HorizontalAlignment="Stretch"
                                             VerticalAlignment="Center" />
            </controls:DataRow>
        </DataTemplate>

        <DataTemplate x:Key="ListItemTemplate" x:DataType="muxc:TreeViewNode">
            <controls:DataRow HorizontalAlignment="Stretch">
                <TextBlock Text="{x:Bind ((models:PlanNodeProperty)Content).Name}"
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Margin="{x:Bind ((models:PlanNodeProperty)Content).Depth, Converter={StaticResource DepthToIndentConverter}, ConverterParameter=4}"
                           VerticalAlignment="Center"
                           TextTrimming="WordEllipsis" />
                <ItemsControl ItemsSource="{x:Bind ((models:PlanNodeProperty)Content).Items}"
                              VerticalAlignment="Center">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Text="{x:Bind}"
                                       FontSize="12"
                                       FontFamily="Consolas"
                                       TextTrimming="WordEllipsis" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </controls:DataRow>
        </DataTemplate>

        <selectors:PlanPropertyTemplateSelector x:Key="PropertyTemplateSelector"
                                                DefaultTemplate="{StaticResource PropertyItemTemplate}"
                                                PredicateTemplate="{StaticResource PredicateItemTemplate}"
                                                ListItemTemplate="{StaticResource ListItemTemplate}" />
    </UserControl.Resources>

    <Grid>
        <TextBlock x:Name="PlaceholderText"
                   Text="No operator selected"
                   FontSize="12"
                   Margin="12,30,12,8"
                   VerticalAlignment="Stretch" />
        <controls:HeaderedTreeView x:Name="TreeView"
                                   SelectionMode="None"
                                   VerticalAlignment="Stretch"
                                   ItemTemplateSelector="{StaticResource PropertyTemplateSelector}">
            <controls:HeaderedTreeView.Header>
                <Border BorderThickness="0,0,0,1">
                    <controls:DataTable ColumnSpacing="2" MinWidth="320" Margin="2">
                        <controls:DataColumn Content="Property" MinWidth="120" CanResize="True" DesiredWidth="170" />
                        <controls:DataColumn Content="Value" MinWidth="140" CanResize="True" DesiredWidth="*" />
                    </controls:DataTable>
                </Border>
            </controls:HeaderedTreeView.Header>
        </controls:HeaderedTreeView>
    </Grid>
</UserControl>
