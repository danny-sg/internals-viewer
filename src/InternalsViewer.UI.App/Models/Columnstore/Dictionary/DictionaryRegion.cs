namespace InternalsViewer.UI.App.Models.Columnstore.Dictionary;

/// <summary>
/// Part of a dictionary blob, each one having a tab of its own
/// </summary>
public enum DictionaryRegion
{
    Header,
    Handles,
    Pages,
    Values
}
