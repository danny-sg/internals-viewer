namespace InternalsViewer.UI.App.ViewModels.Docking;

/// <summary>Identifies which content surface a <see cref="DocumentViewModel"/> hosts.</summary>
public enum DockDocumentKind
{
    Allocation,
    Plan,
    Sql,
    Events
}
