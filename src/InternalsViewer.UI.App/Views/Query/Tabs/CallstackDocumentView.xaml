<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="InternalsViewer.UI.App.Views.Query.Tabs.CallstackDocumentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">
    <controls:DataGrid x:Name="CallstackGrid"
                       ItemsSource="{x:Bind ViewModel.SelectedCallstack, Mode=OneWay}"
                       AutoGenerateColumns="False"
                       IsReadOnly="True"
                       GridLinesVisibility="Horizontal"
                       HorizontalScrollBarVisibility="Auto"
                       VerticalScrollBarVisibility="Auto">
        <controls:DataGrid.Columns>
            <controls:DataGridTextColumn Header="Module"
                                         Binding="{Binding Module, Mode=OneWay}"
                                         Width="100"/>
            <controls:DataGridTextColumn Header="Class"
                                         Binding="{Binding Resolved.ClassName, Mode=OneWay}"
                                         Width="*"/>
            <controls:DataGridTextColumn Header="Method"
                                         Binding="{Binding Resolved.MethodName, Mode=OneWay}"
                                         Width="*"/>
            <controls:DataGridTextColumn Header="Offset"
                                         Binding="{Binding Resolved.Offset, Mode=OneWay}"
                                         Width="80"/>
        </controls:DataGrid.Columns>
    </controls:DataGrid>
</UserControl>
