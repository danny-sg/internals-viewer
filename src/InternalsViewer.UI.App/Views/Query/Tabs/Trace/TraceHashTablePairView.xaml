<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="InternalsViewer.UI.App.Views.Query.Tabs.Trace.TraceHashTablePairView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:trace="using:InternalsViewer.UI.App.Views.Query.Tabs.Trace"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="HashTablePairSplitterStyle" TargetType="toolkit:GridSplitter">
            <Setter Property="Background" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="toolkit:GridSplitter">
                        <Grid Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="180" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MinWidth="180" />
        </Grid.ColumnDefinitions>

        <trace:TraceHashTablePanelView x:Name="LocalPanel"
                                       Grid.Column="0"
                                       Title="Local"
                                       SubTitle="Data Id" />

        <toolkit:GridSplitter Grid.Column="1"
                              Width="1"
                              Style="{StaticResource HashTablePairSplitterStyle}"
                              ResizeBehavior="PreviousAndNext" />

        <trace:TraceHashTablePanelView x:Name="GlobalPanel"
                                       Grid.Column="2"
                                       Title="Global"
                                       SubTitle="Value" />
    </Grid>
</UserControl>
