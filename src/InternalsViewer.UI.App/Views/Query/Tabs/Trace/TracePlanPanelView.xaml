<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="InternalsViewer.UI.App.Views.Query.Tabs.Trace.TracePlanPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:plan="using:InternalsViewer.UI.App.Controls.Plan"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollMode="Enabled"
                  VerticalScrollMode="Enabled"
                  ZoomMode="Enabled"
                  MinZoomFactor="0.25"
                  MaxZoomFactor="2.0"
                  Background="White"
                  Padding="8">
        <plan:ExecutionPlanControl HasFlameGraph="False"
                                   Plan="{x:Bind ViewModel.TracePlan, Mode=OneWay}"
                                   ActiveNodes="{x:Bind ViewModel.ActivePlanNodes, Mode=OneWay}" />
    </ScrollViewer>
</UserControl>
