<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="InternalsViewer.UI.App.Views.Query.Tabs.Trace.TraceTabCommands"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Margin="0,0,8,0">
        <ToggleButton Style="{StaticResource TabCommandToggleStyle}"
                      MinWidth="120"
                      IsChecked="{x:Bind ViewModel.IsResultsVisible, Mode=TwoWay}">
            <TextBlock Text="{x:Bind ViewModel.ResultsLabel, Mode=OneWay}" VerticalAlignment="Center" />
        </ToggleButton>
    </StackPanel>
</UserControl>
